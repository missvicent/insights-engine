Create `__init__.py` files in the specified folder and all its subdirectories.

Target folder: $ARGUMENTS

For every directory found inside the target folder (including the folder itself), create an empty `__init__.py` file if one doesn't already exist. Report which files were created.
